{
  "name": "@slowpost/server",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "prebuild": "yarn workspace @slowpost/data build",
    "build": "tsc -p tsconfig.json",
    "pretest": "yarn workspace @slowpost/data build",
    "test": "vitest run"
  },
  "dependencies": {
    "@slowpost/data": "^0.1.0",
    "express": "^4.19.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.19",
    "typescript": "^5.3.3",
    "vitest": "^1.4.0"
  }
}
